#pragma once

class Menu {
  public:
    static void login();         // 登录界面
    static void main_menu();     // 主菜单
    static void show_course();   // 课程一览
    static void resetpassword(); // 重置密码
    static void course_menu();   // 选课界面
    static void drop_course();   // 退课界面
    static void exit();
};

// 学生选课系统菜单界面